import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'plan-a-frontend-challenge-oluwaseun-odeyemi',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
